name: Moonrepo CI Retrospect
description: Displays the results of `moon ci` in an easy-to-understand format.
inputs:
  access-token:
    description: 'GitHub token for posting PR comments. If not provided, only workflow logs are generated.'
    required: false
outputs: {}
runs:
  using: node20
  main: dist/index.js
